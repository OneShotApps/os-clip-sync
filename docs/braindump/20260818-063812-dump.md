Brain Dump (20260818 063812)

-----

The Clip Sync utility allows the clipboard to be synchronized between multiple Windows and Mac machines.

There is a back-end service that acts as the, I guess, the manager. I would call it a proxy API, but that's, that's not accurate. It is a back-end service that receives API calls from each of the connected clients. The backend API stores all of the clipboard history. So any device that sends a clipboard or item into it, it's stored there for the user.

This is a single-user system, so it is meant to be tied to the user. We offer three, we offer essentially two means of authentication. One is an email-based passwordless login where the user has to receive and enter a six-digit numeric code, and then we're also going to be using Google OAuth or OIDC authentication.

There will be an application that stays running or resident in a system service or a user service, so a user-level service that is running in Windows, a user-level service that is running in Mac, and it essentially synchronizes the clip. Anytime, anytime the user copies something into the clipboard, it is pushed to the API, stored in history. And if there are any other APIs online or any other clients online, it will synchronize and push that down to the running client so that it is automatically in that other client's buffer. Nothing is stored on the client machine itself. All the data is stored in the backend API.

We want the front end to be native, so there needs to be a native Windows application that will run inside of Windows 11 and will be shown as a, you know, system tray object. There's also a version that will run inside of macOS that will be visible in Finder. We will also be creating a mobile application for iOS and a separate mobile application for Android. We'll be using, we'll make sure they're both native and they are using the best technology for that operating system.

We don't care about reusability in this application. Each item should be a fully working entity. It needs to adhere to the overall design considerations of the application, but we're not going to pollute our logic with worrying about the reusability of any one of the application resources.

On the mobile devices that we're going to be creating, they will not be doing a clipboard sync. Instead, they will have a, the logged-in user will be able to see the full clipboard. They'll be able to tap on one of those items and copy it into the buffer.

And there will also be a share-to ability in each of the applications. So on a Windows machine, a user can tap on a photo or they can tap on a piece of text and select share to, and they can choose the Clip Sync utility, at which point it will be pushed to the API and in that situation will automatically be pushed to the buffers on any Windows or Mac machine that is around or that is loaded. The user could also go to the Android machine, do the same thing, or the Android device do the same thing.

And of course they'll be able to tap on any of the items in the window or in the Mac or, I'm sorry, they'll be able to tap on any of the histories in any of the devices, including the mobile and the Windows and the Mac, and they'll be able to click on it to copy it into the buffer so they can paste it again.
