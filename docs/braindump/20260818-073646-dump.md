Brain Dump (20260818 073646)

-----

The approved database for the back-end security and event handling is Postgres. Any type of blob text storage should be in a MongoDB database. Mongoose is authorized as an ORM. Drizzle ORM is authorized as an ORM for Postgres. And Bun are preferred for the API. Flutter will be used for the UI in each of the applications.
